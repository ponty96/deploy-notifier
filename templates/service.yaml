apiVersion: v1
kind: Service
metadata:
  name: {{ include "deploy-notifier.fullname" . }}
  labels:
    {{- include "deploy-notifier.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deploy-notifier.selectorLabels" . | nindent 4 }}
