apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deploy-notifier.fullname" . }}-test-connection"
  labels:
    {{- include "deploy-notifier.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deploy-notifier.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
